Authenticate API. Include API key in Authorization header every request. Prefix API key with "Bearer" space. Authentication fail, server return 401 Unauthorized status code, error message explain fail. API keys sensitive. Never commit version control or share publicly. Store environment variables or use secure secrets management.